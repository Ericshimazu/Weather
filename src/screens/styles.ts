import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    todayTemperature: {
        fontSize: 92,
        marginBottom: 10,
        fontWeight: "bold"
    },
    cityInfo: {
        marginStart: 10,
        marginBottom: 20
    },
    nextDays: {
        marginLeft: 10,
        marginBottom: 20
    }
})

export default Styles